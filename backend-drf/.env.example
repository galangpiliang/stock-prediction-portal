# Example environment variables for the Django backend
# Copy this file to .env and set real values before running the backend.
# Keep the real .env out of version control.

# Secret key used by Django (do NOT commit your real key)
SECRET_KEY=replace-me-with-a-secret-key

# Debug mode (True or False). In production set to False.
DEBUG=True

# Optional: comma-separated allowed hosts
# ALLOWED_HOSTS=localhost,127.0.0.1

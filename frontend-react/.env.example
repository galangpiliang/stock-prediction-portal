# Example environment variables for the frontend (Vite)
# Copy to `.env` at the project root of the frontend and restart the dev server.

# Base API URL for backend requests. Include the API prefix if any.
# Example: http://localhost:8000/api/v1
VITE_BACKEND_BASE_API=http://localhost:8000/api/v1
